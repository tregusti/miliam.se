extends layout

block content
  each entry in entries
    article
      header
        a(href=data2www(entry.path))
          h1= entry.title
      if entry.image
        img(src=data2www(entry.image.thumb), alt=entry.title)
      footer
        time(pubdate)
          = entry.humanDate
          = " "
          = entry.humanTime
      section.body
        != entry.html